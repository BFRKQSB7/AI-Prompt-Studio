# AI-Prompt-Studio 维护清单（index.html）

用途：改 index.html 前快速定位代码。**给模型看，用户不看**。行号随改动漂移，以 grep 锚点为准；改完代码同步更新本表。

## 文件总览
- `index.html`：单文件 Web 应用（HTML+CSS+JS 全内联），中文需求 → SDXL 提示词（Danbooru 标签流 / 自然语言流）+ MiniMax H3 视频提示词。无构建，直接开或静态服务。
- `prompt-db.js`：**外部运行库**，12MB，`var PROMPT_DB=[[中文,英文],...]` 317,423 条，运行时 `<script>` 注入（见 `loadFullLib`），不内联。
- 其余：README/`docs/`=用户文案；`tools/build_db.py`=全量库生成；`opencode_proxy.py`=代理。
- **发布约定**：本清单只入库（上传仓库），**不发布**；改版本/写 changelog 时不要把它当发布内容。

## 版本号 v1.4.0 出现 3 处（改版本必须 `grep v1.4.0` 全改，改完复查零残留）
1. `<title>`（L6）
2. `<h1>` 内 `<span class="ver">`（L88）
3. footer（L266）

## HTML 结构（L1-267）
| 行 | 块 | 关键 id / 锚点 |
|----|----|----------------|
| 7-83 | `<style>` CSS；主题色走 CSS 变量，`:root`(深色) + `[data-theme="light"]` 覆盖 | `--bg --panel --panel2 --border --text --dim --acc --ok --bad` |
| 86-92 | header | `#status`（后端连接点，`pingLocal` 更新）、`#themeBtn` |
| 94-97 | 主 Tab | `#appTabs` → `.tab[data-app=sdxl|h3]` |
| 99-129 | 顶部设置卡 | `#provider #baseUrl #modelName #loadModels #apiKey #apiSite #riskNote`；`#req #reqLab`；`#goBtn #exBtn #err` |
| 131-181 | SDXL 面板 `#sdxlPanel` | `#modeRadios`(`.radio[data-mode=nl|tag]`)、`#nsfw`+`#nsfwSlider`(自绘滑条，含 `#nsfwFill #nsfwThumb`)、`#libRadios`(`.radio[data-lib=off|cur|full]`)、`#libStatus`、`#tagLimit`(上限，默认20)、`#maxTok`(默认2048) |
| 183-234 | H3 面板 `#h3Panel` | `#h3GenRadios`(`[data-gen=t2va|i2va|fl2va|l2va|ref2va]`)、`#h3ModeRadios`(`[data-h3=drama|action|grid]`)、`#h3Dur`、`#h3MaxTok`(默认2048，独立于 maxTok)、`#h3Refs #h3RefsLab` |
| 236-264 | 输出卡 | `#sdxlOut`：`#out #tagCaps #copyBtn #clearBtn #negGenChk #neg #negCaps #copyNegBtn #clearNegBtn`；`#h3Out`：`#out3 #copy3Btn #clear3Btn` |

> 新增控件流程：HTML 加 id → `cfg` 默认值(L278) → `applyCfg`(L286)/`saveCfg`(L301) 读写 → 需要时加事件监听。

## JS 模块（L269-1041）
| 行 | 模块 | grep 锚点 | 逻辑要点 |
|----|------|-----------|----------|
| 270-278 | 基础 | `$=` `LS=` `PROVIDERS=` `let cfg=` | `$`=getElementById；cfg 存 `localStorage('promptConvCfg')`，默认 `{provider,base,model,key,lib,negGen,tagLimit,maxTok,h3MaxTok}` |
| 280-348 | 配置/模型 | `applyCfg` `saveCfg` `loadModels` `API_SITES` | loadModels fetch `${base}/models`；**local 源剥 `models/` 前缀+路径**只留干净模型名；provider/baseUrl/modelName/apiKey 的 change 都 saveCfg；local 切非 custom 时自动回填 PROVIDERS 地址 |
| 350-375 | 直白词典 | `const DICT` | 中文→英文 NSFW 映射（含摄影/质量词），`buildSystemPrompt` 引用 |
| 377-378 | 精选库 | `const CURATED_LIB` | 内嵌 347 条 `[[zh,en],...]` |
| 380-383 | 直白度 | `const NSFW_LV` | 0保守/1标准/2直白/3高度直白 |
| 385-418 | 系统提示词 | `buildSystemPrompt(mode,lv,refBlock)` | mode=tag(Danbooru 标签)/nl(自然语言)；拼 lvLine+dictText+示例+(refBlock)；**结尾追加 refBlock** |
| 420-433 | 负面提示词 | `buildNegSystemPrompt` `sanitizeNeg` `NEG_FORBID` | NEG_FORBID 是**禁止把审查/安全术语写进负面**的硬约束（会抑制露骨内容）；sanitizeNeg 按 NEG_FILTER 清除+并多余逗号 |
| 434-451 | 正向后筛选 | `sanitizeTags` `KEEP_QUALITY` | **依赖 FULL_LIB**（未加载直接返回原样）；tag 模式+全量库在 → 把输出里不在库的标签剔除，KEEP_QUALITY 白名单兜底 |
| 452-558 | 中文胶囊 | `renderTagCaps` `splitTopLevel` `makeDraggable` `showWeightEditor` `EN_ZH` | splitTopLevel 按 **depth=0 的逗号**切分（括号内逗号不算）；胶囊翻译 EN_ZH 顺序 NEG_ZH→LIB_CUR→FULL_LIB（FULL 最后写入=全量优先），**未加载则后台 `loadFullLib().then(重渲染)`**；×删除/右键改权重/拖拽排序 |
| 560-681 | 提示词库检索 | `retrieveTags` `buildKeysBy2` `loadFullLib` `buildReferenceBlock` `CHAR_TAGS` `disambigScore` | 结构=**2 字分桶**前缀索引（实测 Trie 1.7M 节点 ~166MB 不可行）；输入按 CJK 段滑窗：精确子串(cp≥len) 长键优先、前缀扩展每位置封顶 8 条；扩展排序=系列括号加权 > curated 位 > cp 深 > 短键；总输出 cap=40（`buildReferenceBlock` 传 40）；`CHAR_TAGS` 命中 cleanInput（剥 空格/·、，。；的）→ 置顶注入 |
| 683-777 | H3 提示词 | `buildH3SystemPrompt` `postProcessH3` `h3DurData` | 英文结构化（base=三字段 integrated_multimodal_description/overall_soundscape/non_diegetic_music，Ref2VA=六段）；postProcessH3 截断指令泄漏标记+补真实时间戳+清骨架占位+规整对白 |
| 779-877 | 交互监听 | `syncLibUI` `updateH3RefsUI` `H3_REFS_UI` | 全局状态 `mode/libMode/app/h3Mode/h3Gen`；appTabs 切 sdxl/h3 显隐面板与输出卡；nsfwSlider 自绘滑条(mousedown+touchstart 拖拽) |
| 879-887 | 后端探测 | `pingLocal` | **硬编码 `http://127.0.0.1:4001/v1/models`**，每 6s `setInterval`（用户实测常改 4000，改测试端口在 Base URL 框，勿改 pingLocal） |
| 889-901 | 转换入口 | `goBtn` 监听 | 校验 req/base/model/key → `app==='h3' ? doH3 : doSdxl` |
| 903-946 | 流式生成 | `streamChat(base,model,key,messages,temperature,maxTokens,tagLimit)` | SSE 流式 + `chat_template_kwargs:{enable_thinking:false}`；**按 depth=0 的逗号实时计数**，≥tagLimit → `ctrl.abort()` + `text.replace(/,\s*$/,'')` 精确停在第 N 个标签 |
| 948-987 | SDXL 主流程 | `doSdxl` | loadFullLib(全量按需)→buildReferenceBlock→streamChat(正, max_tokens=maxTok)→tag 模式+FULL_LIB 则 sanitizeTags→renderTagCaps→`negGenChk` 勾选则再 streamChat(负) 否则填 DEFAULT_NEG_*→renderTagCaps(负) |
| 989-1010 | H3 主流程 | `doH3` | 非流式 fetch，`AbortSignal.timeout(180000)`，max_tokens=h3MaxTok，postProcessH3 收尾 |
| 1012-1028 | 复制/清空 | `bindCopy` `bindClear` | 复制走 `navigator.clipboard` + execCommand 兜底；清空=value+caps |
| 1030-1041 | 杂项/启动 | `exBtn` `themeBtn`；初始化行(L1040) `applyCfg(); pingLocal(); updateH3RefsUI(); setNsfw(...)` | 主题存 html `data-theme` |

## 关键约定 / 坑
- **本地后端端口**：UI 默认 4001（cfg baseUrl 默认 + pingLocal 硬编码 4001）；改测试端口在 Base URL 框，`pingLocal` 是探测用别改。
- **本地模型名**：loadModels 对 local 源剥 `models/`+路径只留干净文件名。
- **强制停止**：tagLimit 计数在 `streamChat` 内（depth=0 逗号才算），到限 abort+去尾逗号；正负面分开各限各的。
- **全量库只在浏览器内存检索**，每轮只发 ≤cap 条命中，不占模型上下文；首次约 11.5MB 下载。
- **胶囊翻译全量优先**：buildEnZh 写序 NEG_ZH→LIB_CUR→FULL_LIB，FULL 后写覆盖=全量优先；renderTagCaps 里未加载全量库时后台拉取后重渲染。
- **角色消歧**：`CHAR_TAGS` 手工表（如 `无职转生艾莉丝→eris_greyrat`），新版权角色检索不对时照格式加一条；`disambigScore` 对 `(系列)` 后缀命中输入→加权（希露菲叶特 (无职转生) 对"无职转生希露菲"）。数据库本身大量角色带 `(系列)` 括号消歧键。
- **负面禁审查词**：NEG_FORBID 写入系统提示词是设计（防止负面里带 censorship 抑制露骨内容），勿改；NEG_FILTER 兜底清除。
- **NSFW 直写为设计意图**：直白词典/DEFAULT/示例含成人词，用户自用合法内容，**勿"净化"、勿加审查过滤**。
- **H3 非流式 + 180s 超时**；h3MaxTok 独立于 maxTok（H3 结构化输出更吃 token）。
- **cfg 字段 clamp**：saveCfg 里 tagLimit 1-100(默认20)、maxTok 64-8192(默认2048)、h3MaxTok 64-16384(默认2048)；lib 三态 off/cur/full；negGen 布尔(默认 true=模型生成负面)。
